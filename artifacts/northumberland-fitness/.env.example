# URL of the deployed API server (see /server). Leave blank during local
# development against a local API on the same host, or point it at your
# Render/Railway URL, e.g. https://northumberland-fitness-api.onrender.com
VITE_API_URL=
